import React from "react";

// Components
import Tile from "../Tile";

export default function Patrol () {
  return (
    <div>
      <Tile status={'ship'}/>
      <Tile status={'ship'}/>
    </div>
  );
}